import React from 'react';
import { motion } from 'framer-motion';

const Profile = () => {
    const v1 = 1;
    return (
        <div>
            profile page
        </div>
    );
};

export default Profile;