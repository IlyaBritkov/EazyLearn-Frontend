import React from 'react';
import { motion } from 'framer-motion';

const Favourite = () => {
    const v1 = 1;
    return (
        <div>
            favoruite page
        </div>
    );
};

export default Favourite;